# Optional — only if running gold-price-scraper/ yourself (legacy).
# Production uses scripts/fetch_gold_quote.py instead.
DATABASE_URL=postgresql://user:password@ep-xxx.neon.tech/dbname?sslmode=require
# If set, api/scrape-gold.js requires Authorization: Bearer <value> on requests.
CRON_SECRET=

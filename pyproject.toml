[project]
name = "imprint-website-worker"
version = "0.1.0"
description = "Cloudflare Python Worker stub (full site on Render)"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "workers-py>=1.90",
    "workers-runtime-sdk",
]

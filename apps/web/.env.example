# Server-side fetch to FastAPI (RSC / Route Handlers). No CORS needed.
API_INTERNAL_BASE=http://127.0.0.1:8080

# Browser → Next rewrite target (next.config.ts). Same host as API in prod gateway.
API_PROXY_TARGET=http://127.0.0.1:8080

"""Database connection placeholder.

No persistence layer exists yet — dynamic data comes from the live gold-price
scrape (app/bot_gold.py) and the separate Vercel API in backend/. Add a
SQLAlchemy engine/session factory here if the FastAPI app ever needs its own
database.
"""
